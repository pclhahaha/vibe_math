# 人工抽验清单（浏览器验收用）

按优先级挑代表性页面检查：

1. **本批新增节是否位置自然**（在"前置知识"之前、编号不跳、无重复标题）：
   complex-plane · entropy · ising-model · standard-model · turing-machine · set-theory
2. **公式渲染无红字**：打开后 Ctrl+F5，扫一遍含 `关键论文` 页的正文公式。
3. **画布交互仍正常**（新增节不影响 draw）：随机抽查 5 个画布拖动/按钮。
4. **图谱筛选**：分类+难度双筛选后节点数合理。
5. **资格考试页**：35 题课程互链跳转正常。
6. **手机宽度**：等比缩放后控件可用。

若发现异常，把页面 URL + 现象发我即可。
